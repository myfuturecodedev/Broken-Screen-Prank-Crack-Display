<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="20dp">

    <!-- 1. Layered Illustration Stack Frame -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/illustrationContainer"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="20dp"
        android:layout_marginBottom="32dp"
        app:layout_constraintTop_toTopOf="parent">

        <!-- Background Glass Cracks vector layers directly back of illustrations -->
        <ImageView
            android:id="@+id/ivBackgroundCrack"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:src="@drawable/onboard_one" />

        <!-- Centered Character + Phone Art Layer -->
<!--        <ImageView-->
<!--            android:id="@+id/ivMainIllustration"-->
<!--            android:layout_width="0dp"-->
<!--            android:layout_height="match_parent"-->
<!--            android:scaleType="fitCenter"-->
<!--            app:layout_constraintEnd_toEndOf="parent"-->
<!--            app:layout_constraintStart_toStartOf="parent" />-->

    </androidx.constraintlayout.widget.ConstraintLayout>


    <!-- 2. White Primary Headings -->
    <TextView
        android:id="@+id/tvTitleLine1"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:lineHeight="32dp"
        android:textColor="@color/lang_text_white"
        android:textSize="26sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toTopOf="@id/tvTitleLine2" />

    <!-- 3. Vibrant Neon Cyan Subtitles -->
    <TextView
        android:id="@+id/tvTitleLine2"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:gravity="center"
        android:lineHeight="32dp"
        android:textColor="@color/lang_cyan_glow"
        android:textSize="26sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toTopOf="@id/tvDescription" />

    <!-- 4. Muted Slate Gray Description Copy -->
    <TextView
        android:id="@+id/tvDescription"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="20dp"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="16dp"
        android:gravity="center"
        android:lineHeight="22dp"
        android:textColor="@color/lang_text_muted"
        android:textSize="15sp"
        app:layout_constraintBottom_toBottomOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>